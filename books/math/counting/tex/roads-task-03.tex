\documentclass[tikz,border=6pt]{standalone}

% общий стиль (подключается через TEXINPUTS)
\input{tikz-style.tex}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm, line cap=round, line join=round]

\tikzset{
  main/.style={draw=black, line width=0.85pt},
  vtx/.style={circle, draw=darkgray, fill=vtxfill, line width=0.85pt, minimum size=10pt, inner sep=0pt},
  vlab/.style={font=\Large},
}

\coordinate (P) at (0,0);
\coordinate (A) at (2.8,3.0);
\coordinate (B) at (2.8,-3.0);
\coordinate (Q) at (6.2,0);

% edges P-A (2) — left side (outer + inner)
\draw[main] (P) to[out=140,in=180,looseness=1.25] (A);
\draw[main] (P) to[out=60,in=210,looseness=0.95] (A);

% edges A-Q (3) — right side (outer + two inner)
\draw[main] (A) to[out=20,in=70,looseness=1.3] (Q);
\draw[main] (A) to[out=-10,in=100,looseness=1.05] (Q);
\draw[main] (A) to[out=-35,in=130,looseness=0.95] (Q);

% edges P-B (3) — left side (outer + two inner)
\draw[main] (P) to[out=220,in=200,looseness=1.25] (B);
\draw[main] (P) to[out=260,in=170,looseness=0.95] (B);
\draw[main] (P) to[out=300,in=150,looseness=0.95] (B);

% edges B-Q (2) — right side (outer + inner)
\draw[main] (B) to[out=-20,in=-70,looseness=1.3] (Q);
\draw[main] (B) to[out=10,in=-110,looseness=1.05] (Q);

% vertices
\node[vtx] at (P) {};
\node[vtx] at (A) {};
\node[vtx] at (B) {};
\node[vtx] at (Q) {};

% labels
\node[vlab, left=8pt] at (P) {P};
\node[vlab, above=6pt] at (A) {A};
\node[vlab, below=6pt] at (B) {B};
\node[vlab, right=6pt] at (Q) {Q};

\end{tikzpicture}
\end{document}
