\documentclass[aspectratio=169]{beamer}

% убрать навигационные значки beamer (иконки в правом нижнем углу)
\setbeamertemplate{navigation symbols}{}

\input{tikz-style.tex}

\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,trees,overlay-beamer-styles}

% ---------- styles ----------
\tikzset{
    letterBase/.style={circle, minimum size=9mm, inner sep=0pt, text=white},
    Aletter/.style={letterBase, fill=darkred},
    Bletter/.style={letterBase, fill=darkgreen},
    Cletter/.style={letterBase, fill=darkblue},
    word/.style={font=\bfseries\Large, text=black, draw=darkgray, rounded corners=2pt,
    inner xsep=5pt, inner ysep=8pt}, % "воздух" в листьях
    edgeBase/.style={draw=darkgray, -{Stealth[length=2.0mm]}, line width=0.45pt,
    shorten >=2pt, shorten <=2pt} % тоньше стрелки
}

% ---------- global scale (one knob: shrink everything uniformly) ----------
\def\TreeScale{0.82}

\begin{document}

% Один кадр Beamer = один шаг.
% Элементы показываем "видимы с шага N": visible on=<N->
    \begin{frame}[plain]
    \centering
    \begin{tikzpicture}[
        scale=\TreeScale, transform shape,
        line cap=round, line join=round,
        grow=down,
        level distance=13mm,
        edge from parent/.style={edgeBase},
        every node/.style={font=\bfseries\Large, align=center},
        level 1/.style={sibling distance=52mm},
        level 2/.style={sibling distance=26mm},
        level 3/.style={sibling distance=13mm},
        level 4/.style={sibling distance=13mm}
    ]

% ---------- tree ----------
        \node[coordinate] {}
        child[visible on=<2->] { node[Aletter] {М}
        child[visible on=<3->] { node[Bletter] {С}
        child[visible on=<4->] { node[Cletter] {Я}
        child[visible on=<5->] { node[word] {МСЯ} }
        }
        }
        child[visible on=<6->] { node[Cletter] {Я}
        child[visible on=<7->] { node[Bletter] {С}
        child[visible on=<8->] { node[word] {МЯС} }
        }
        }
        }
        child[visible on=<9->] { node[Bletter] {С}
        child[visible on=<10->] { node[Aletter] {М}
        child[visible on=<11->] { node[Cletter] {Я}
        child[visible on=<12->] { node[word] {СМЯ} }
        }
        }
        child[visible on=<13->] { node[Cletter] {Я}
        child[visible on=<14->] { node[Aletter] {М}
        child[visible on=<15->] { node[word] {СЯМ} }
        }
        }
        }
        child[visible on=<16->] { node[Cletter] {Я}
        child[visible on=<17->] { node[Aletter] {М}
        child[visible on=<18->] { node[Bletter] {С}
        child[visible on=<19->] { node[word] {ЯМС} }
        }
        }
        child[visible on=<20->] { node[Bletter] {С}
        child[visible on=<21->] { node[Aletter] {М}
        child[visible on=<22->] { node[word] {ЯСМ} }
        }
        }
        };

% ---------- level labels (left) ----------
% Показываем подписи уровней всегда, верхние не делаем.
        \path (current bounding box.north west) coordinate (bbNW);
        \node[font=\bfseries\Large, anchor=east] at ($(bbNW)+(-6mm,-13mm)$) {1-й};
        \node[font=\bfseries\Large, anchor=east] at ($(bbNW)+(-6mm,-26mm)$) {2-й};
        \node[font=\bfseries\Large, anchor=east] at ($(bbNW)+(-6mm,-39mm)$) {3-й};

    \end{tikzpicture}
    \end{frame}

\end{document}