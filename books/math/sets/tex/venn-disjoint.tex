\documentclass[tikz,border=6pt]{standalone}
\usepackage{amssymb}

% общий стиль (подключается через TEXINPUTS)
\input{tikz-style.tex}

\begin{document}
  \begin{tikzpicture}[line cap=round,line join=round]

% --- geometry ---
    \def\R{2.15}                    % circle radius
    \coordinate (L)   at (-2.45,0.20); % left circle center
    \coordinate (Rgt) at ( 2.45,0.20); % right circle center

% universe rectangle
    \draw[line width=1.0pt] (-5.5,-2.95) rectangle (5.5,2.95);

% circles
    \draw[line width=1.0pt] (L) circle (\R);
    \draw[line width=1.0pt] (Rgt) circle (\R);

% labels
    \node[lab] at ($ (L) + (-2.15,1.70) $) {$A$};
    \node[lab] at ($ (Rgt) + ( 2.15,1.70) $) {$B$};

% universe label (outside bottom-left of the rectangle)
    \node[lab, anchor=north east] at (-5.5,-2.35) {$U$};

% caption below (centered under the rectangle)
    \node[lab] at (0,-3.55) {$A\cap B = \varnothing$};

  \end{tikzpicture}
\end{document}