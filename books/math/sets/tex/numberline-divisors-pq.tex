\documentclass[tikz,border=6pt]{standalone}

% общий стиль (подключается через TEXINPUTS)
\input{tikz-style.tex}

\usepackage{tikz}
\usetikzlibrary{arrows.meta}

\begin{document}
\begin{tikzpicture}[x=0.2cm,y=1cm, line cap=round, line join=round]

\draw[->, line width=0.9pt] (-1,0) -- (41,0);

\foreach \x in {0,10,20,30,40} {
  \draw (\x,0.12) -- (\x,-0.12);
  \node at (\x,-0.6) {\x};
}

\def\yP{0.35}
\def\yQ{0.70}
\def\labelscale{0.80}
\pgfmathsetlengthmacro{\labelgap}{1.2ex}

% P: divisors of 28
\foreach \x in {1,2,4,7,14,28} {
  \fill[midblue] (\x,\yP) circle (2.0pt);
}

% Q: divisors of 40
\foreach \x in {1,2,4,5,8,10,20,40} {
  \fill[midred] (\x,\yQ) circle (2.0pt);
}

\node[color=midblue, font=\normalsize, scale=\labelscale] at ($(14,\yP)+(0,\labelgap)$) {P};
\node[color=midred, font=\normalsize, scale=\labelscale] at ($(20,\yQ)+(0,\labelgap)$) {Q};

\end{tikzpicture}
\end{document}
