\documentclass[tikz,border=8pt]{standalone}

% общий стиль (подключается через TEXINPUTS)
\input{tikz-style.tex}

\usetikzlibrary{arrows.meta}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm,line cap=round,line join=round]

% main number line
\draw[line width=1.0pt, <->, >=Stealth] (-7,0) -- (7,0);

% integer ticks and labels
\foreach \x in {-6,-5,...,6} {
  \draw[line width=1.0pt] (\x,0.12) -- (\x,-0.12);
  \node[font=\Large] at (\x,-0.55) {$\x$};
}

% red direction arrow above (continuous)
\draw[darkred, line width=1.2pt, <->, >=Stealth] (-6.7,0.85) -- (6.7,0.85);

\end{tikzpicture}
\end{document}